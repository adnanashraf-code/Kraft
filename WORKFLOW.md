# KRAFT Project Workflow

This document explains the internal architecture and development workflow of the KRAFT Design Editor.

## 🏗️ System Overview

The application follows a centralized state management pattern using **Zustand**. The UI is decoupled from the logic, allowing for a highly responsive and predictable design environment.

```mermaid
graph TD
    App[App.jsx] --> Routes{Routes}
    Routes --> Landing[Landing Page]
    Routes --> Dash[Dashboard]
    Routes --> Editor[Design Editor]
    
    Editor --> Canvas[Canvas Component]
    Editor --> Sidebar[Properties Sidebar]
    Editor --> Toolbar[Main Toolbar]
    Editor --> Overlays[Modals & Overlays]
    
    Canvas <--> Store((Zustand Store))
    Sidebar <--> Store
    Toolbar <--> Store
    Overlays <--> Store
    
    Store --> Persistence[LocalStorage]
    Store --> History[Undo/Redo Stack]
```

## 🎨 Asset Pipeline

KRAFT manages thousands of assets (icons, fonts, shapes) through a unified resolution pipeline.

```mermaid
sequenceDiagram
    participant User
    participant Modal as AssetsModal
    participant Utils as iconUtils.js
    participant Store as useEditorStore
    participant Canvas as Editor Canvas

    User->>Modal: Opens Asset Vault
    User->>Modal: Searches for "Home"
    Modal->>Utils: resolveIcon("Home")
    Utils-->>Modal: Returns Lucide/Remix SVG
    User->>Modal: Clicks "Insert"
    Modal->>Store: addElement({type: 'icon', name: 'Home', ...})
    Store->>Store: Save History
    Store-->>Canvas: Re-renders with new Element
```

## 🔄 State & History Flow

The "Heart" of KRAFT is the `useEditorStore`. It manages the document structure and maintains a high-fidelity history stack.

```mermaid
graph LR
    Action[User Action] --> Dispatch[Store Action]
    Dispatch --> Save[Save History]
    Save --> Pages[Update Pages Array]
    Pages --> UI[Notify UI Components]
    
    subgraph History Management
        Save --> Past[Past Stack]
        Undo[Undo Action] --> Past
        Undo --> Future[Future Stack]
    end
```

## 🛠️ Development Workflow

1.  **State First**: Define the required state and actions in `useEditorStore.js`.
2.  **Logic Integration**: Implement utility functions in `src/utils/` for complex operations (e.g., coordinate calculations).
3.  **UI Implementation**: Create/Update components in `src/components/` using Tailwind CSS for styling.
4.  **Theme Verification**: Ensure the new feature works across all KRAFT themes (Light, Dark, Gray).
5.  **Documentation**: Update `IMPLEMENTATION_PLAN.md` with the new progress.

---
*Generated by Antigravity AI*
